export const SCHEMA=[`CREATE TABLE IF NOT EXISTS guild_settings(guild_id TEXT PRIMARY KEY,settings_json TEXT NOT NULL DEFAULT '{}')`,`CREATE TABLE IF NOT EXISTS notification_subscriptions(id INTEGER PRIMARY KEY AUTOINCREMENT,guild_id TEXT NOT NULL,source_key TEXT NOT NULL,channel_id TEXT NOT NULL,enabled INTEGER NOT NULL DEFAULT 1,keywords TEXT DEFAULT '[]')`,`CREATE TABLE IF NOT EXISTS notification_items(source_key TEXT NOT NULL,item_key TEXT NOT NULL,title TEXT,url TEXT,published_at INTEGER,PRIMARY KEY(source_key,item_key))`,`CREATE TABLE IF NOT EXISTS command_cooldowns(guild_id TEXT,user_id TEXT,command TEXT,expires_at INTEGER,PRIMARY KEY(guild_id,user_id,command))`,`CREATE TABLE IF NOT EXISTS reminders(id INTEGER PRIMARY KEY AUTOINCREMENT,guild_id TEXT,user_id TEXT,channel_id TEXT,message TEXT,run_at INTEGER,enabled INTEGER DEFAULT 1)`,`CREATE TABLE IF NOT EXISTS afk(guild_id TEXT,user_id TEXT,message TEXT,created_at INTEGER,PRIMARY KEY(guild_id,user_id))`];