export function subscribe(db,guildId,sourceKey,channelId,keywords=[]){return db.prepare("INSERT INTO notification_subscriptions(guild_id,source_key,channel_id,keywords) VALUES(?,?,?,?)").run(guildId,sourceKey,channelId,JSON.stringify(keywords))}export function listSubscriptions(db,guildId){return db.prepare("SELECT * FROM notification_subscriptions WHERE guild_id=?").all(guildId)}