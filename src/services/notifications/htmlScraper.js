import * as cheerio from "cheerio";import crypto from "node:crypto";import {getText} from "./http.js";export async function scrapeLinks(source){const $=cheerio.load(await getText(source.url)),out=[];$("a[href]").each((_,el)=>{const title=$(el).text().replace(/\s+/g," ").trim(),href=$(el).attr("href");if(!href||title.length<8)return;let url;try{url=new URL(href,source.url).href}catch{return}if(out.some(x=>x.url===url))return;out.push({key:crypto.createHash("sha256").update(url).digest("hex"),title,url,description:title})});return out.slice(0,50);}