import {EmbedBuilder} from "discord.js";export const baseEmbed=(t,d="")=>new EmbedBuilder().setTitle(t).setDescription(d).setTimestamp().setFooter({text:"Nararya Bot Discord"});export const notificationEmbed=(x,s)=>{const e=baseEmbed("📢 "+s.name,x.title).setURL(x.url);if(x.thumbnail)e.setThumbnail(x.thumbnail);return e.addFields({name:"Sumber",value:s.name,inline:true},{name:"Waktu",value:`<t:${Math.floor(x.publishedAt/1000)}:R>`,inline:true});};