import 'dotenv/config'; import {REST,Routes} from 'discord.js'; import {commandData} from './index.js'; const rest=new REST({version:'10'}).setToken(process.env.DISCORD_TOKEN); await rest.put(Routes.applicationCommands(process.env.CLIENT_ID),{body:commandData.map(x=>x.toJSON())}); console.log('Commands deployed');
