import test from "node:test";import assert from "node:assert/strict";import {levelFromXP} from "../src/services/community/levels.js";import {dice,coinflip,rps} from "../src/services/games/index.js";test("level calculation",()=>{assert.equal(levelFromXP(0),0);assert.equal(levelFromXP(100),1);assert.equal(levelFromXP(400),2)});test("dice range",()=>{for(let i=0;i<50;i++){const n=dice();assert.ok(n>=1&&n<=6)}});test("rps",()=>{assert.equal(rps("rock","scissors"),"win");assert.equal(rps("rock","rock"),"draw")});test("coinflip",()=>assert.ok(["heads","tails"].includes(coinflip())));